version: 2.1
orbs:
  cypress: cypress-io/cypress@2.0.0
  workflows:
    example-workflow:
      jobs:
        - cypress/run:
            executor: base-14 
            parallel: true 
            parallelism: 1 
            browser: electron 